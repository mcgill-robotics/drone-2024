# Package responsible for understanding mission boundaries, managing current state and ordering other nodes.
## Dependencies
### pymap3d
Helps transform global coordinates to local coordinates. 
Run `pip install pymap3d` to install.
